function solve(input) {

    input = math.matrix([20, 40], [10, 60]);
    console.log(input);


    // for (let i = 0; i < input.length; i++) {
    //     let currArr = input[i];
    //     currArr.forEach(el => {
    //         newArr.push(el);
    //     });
    // }
    // let main = 0;
    // let secondary = 0;
    // let result = [];
    // newArr.forEach((el) => {
    //     if (newArr.indexOf(el) % 2 === 0) {
    //         main += el;
    //     } else {
    //         secondary += el;
    //     }
    // });
    // result.push(main, secondary);
    // console.log(result.join(" "));




}
solve(
    [[20, 40],
    [10, 60]]);